# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests, `MODEL_CARD.md`, and the standalone `TASK-INFERENCE` tutorial `tutorials/chronos_2_forecasting_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is recorded in `docs/release-verification.md`. The earlier repository-installing notebook's CI execution record (real pinned weights on `main`) predates the standalone carrier and does not count for it.
